# IP Network Info

Простой проект для проверки IP адресов

## Features (Основные функции)
- Validate IP address
- обнаружение IPv4 or IPv6
- узнать IP является приватным или global
- loopback status

## Run (Запуск)
```bash
pip install -r requirements.txt
python main.py